function oblicz(){
    let a1=parseInt(document.getElementById("liczba").value);
    let Fn=0;
    if(n<=2){
        Fn=an;
    }else{
        let (an-a1);
        let (n-1);
        while(i<=n){
            (an-a1)/(n-1);
        }
    }
    document.getElementById("wynik").value=Fn;
    }
